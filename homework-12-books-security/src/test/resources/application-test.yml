spring:
  datasource:
    url: jdbc:h2:mem:books_test
    initialization-mode: always

  liquibase:
    enabled: true
    drop-first: true

  jpa:
    hibernate:
      ddl-auto: none
logging:
  level:
    org.springframework: "info"
    ru.otus.spring: "info"

