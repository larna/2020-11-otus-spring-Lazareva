spring:
  datasource:
    url: jdbc:h2:mem:books_test
    initialization-mode: always
#    data: data-test.sql

  liquibase:
    enabled: true
    drop-first: true

  output:
    ansi:
      enabled: always
  shell:
    interactive:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: none
logging:
  level:
    org.springframework: "info"
    ru.otus.spring: "info"

